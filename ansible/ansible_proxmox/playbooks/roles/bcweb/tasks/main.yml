---
# tasks file for bcweb
- name: Instalar HaProxy
  ansible.builtin.apt:
    name: haproxy
    state: present
    update_cache: yes
- name: Copiar archivo de configuracion
  ansible.builtin.copy:
    src: haproxy.cfg
    dest: /etc/haproxy/haproxy.cfg
  notify: reinicio_haproxy